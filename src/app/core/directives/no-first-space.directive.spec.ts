import { NoFirstSpaceDirective } from './no-first-space.directive';

describe('NoFirstSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NoFirstSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
